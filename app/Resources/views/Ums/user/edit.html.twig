{% extends 'base.html.twig' %}

{% block headtitle %}Users Edit{% endblock %}
{% block body %}
    {% if user.pru.pruName == 'ROLE_ADMIN' or (user.pru.pruName == 'ROLE_AGENT' and user.usrGrantList) %}
    <ul>
        <li>
            <a class="btn btn-primary" href="{{ path('Ums_index') }}">Back to the list</a>
        </li>
    </ul>
    {% endif %}
    
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br/>
        <input class="btn btn-info btn-lg" type="submit" value="Edit" />
    {{ form_end(edit_form) }}
{% endblock %}
